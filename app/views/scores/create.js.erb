$('.profile').replaceWith("<%= escape_javascript(render(:partial => 'players/profile')) %>");
$('#players').html("<%= escape_javascript(render(Player.order('rating DESC'))) %>");
$('.modal').modal('hide').remove()
