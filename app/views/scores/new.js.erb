var game_form = "<%= escape_javascript(render(:template => 'scores/new', :formats => [:html], :layout => nil)) %>"

$('.modal').empty()
$('body').append(game_form)
$('.modal').modal({
  backdrop: 'static',
  keyboard: false
});
