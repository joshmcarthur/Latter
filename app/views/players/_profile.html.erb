<div class="profile">
  <div class="thumbnail">
    <%= image_tag current_player.gravatar_url(:size => 300), :title => current_player.name %>
  </div>

  <div class="info">
	
    <h2><%= current_player.name %></h2>
    <h3><%= I18n.t('player.points', :points => current_player.rating) %></h3>
    <%= ranking(current_player) %>
    <h3 class="visible-tablet visible-desktop"><%= current_player.games.count %></h3>
	<% if current_player.badges.any? %>
    	<%= render partial: 'badges/player_badge', collection: current_player.badges %>
 	<% end %>
  </div>
</div>

