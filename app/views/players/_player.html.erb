<%# cache cache_key_for(player, "listing-#{current_player ? 'logged-in' : 'anonymous'}") do %>
  <li class="row-fluid player <%= player_class(player) %>" id="<%= dom_id(player) %>">
    <div class="spanX rank-wrapper">
      <div class="rank">
        <%= player.ranking %>
      </div>
    </div>
    <div class="spanX">
      <div class="avatar-wrapper">
        <%= link_to_primary_action_for player, :class => 'avatar avatar64' do %>
          <%= image_tag player.gravatar_url(:size => 64) %>
        <% end %>
      </div>
    </div>
    <h3 class="name spanX"><%= player.name %></h3>
    <div class="points span2">

      <div class="value"><%= player.rating %><%= trend(player) %></div>
      <div class="label">
        Points
      </div>
    </div>
    <!--
      <div class="row-fluid data_and_actions">
        <div class="span5 points_display">
          <h4><%= I18n.t('player.points', :points => player.rating) %></h4>
        </div>
        <div class="span5 controls">
          <% if current_player %>
            <div class="btn-group">
              <%= link_to player, :class => 'btn btn-large' do %>
                <i class='icon-user'></i>
                <%= I18n.t('player.view') %>
              <% end %>
              <%= link_to_primary_action_for(player, :class => 'btn btn-large') %>
            </div>
          <% end %>
        </div>
        <div class="span2 ranking_display">
          <%= ranking(player) %>
        </div>
      </div>
    </div> -->
  </li>
<%# end %>

