<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= content_for?(:title) ? yield(:title) : "Latter" %></title>
    <%= csrf_meta_tags %>
    <!-- Le HTML5 shim, for IE6-8 support of HTML elements -->
    <!--[if lt IE 9]>
      <script src="http://html5shim.googlecode.com/svn/trunk/html5.js" type="text/javascript"></script>
    <![endif]-->
    <%= stylesheet_link_tag "application", :media => "all" %>
    <%= favicon_link_tag %>


  </head>
  <body class="tk-lint-mccree">
    <%= render 'layouts/navigation' %>
    <div class="container">
      <div class="content" data-pjax-container>
        <div class="row-fluid">
          <% if content_for?(:sidebar) %>
            <div class="span3 sidebar">
              <%= yield :sidebar %>
            </div>
          <% end %>
          <div class="span9">
            <% flash.each do |key, value| %>
              <%= content_tag :div, :class => "alert alert-#{key}" do %>
                <button class="close" data-dismiss="alert">×</button>
                <strong><%= key.to_s.humanize %>:</strong>
                <%= value %>
              <% end %>
            <% end %>
            <%= yield %>
          </div>
        </div><!--/row-fluid-->
      </div><!--/content-->
    </div> <!-- /container -->
    <!-- Javascripts
    ================================================== -->
    <!-- Placed at the end of the document so the pages load faster -->
    <%= javascript_include_tag "application" %>

    <script>
      $(function(){
        $('a:not([data-remote]):not([data-behavior]):not([data-skip-pjax])').pjax('[data-pjax-container]')
      })
    </script>

    <script type="text/javascript" src="//use.typekit.net/qet3ipw.js"></script>
    <script type="text/javascript">try{Typekit.load();}catch(e){}</script>
  </body>
</html>
