.page-header
  %h1 Challenges

#games
  - if @games.empty?
    .alert.alert-info
      %i.icon-info-sign
      No challenges right now...
  - @games.each do |game|
    = haml(:"games/game_row", :locals => {:game => game})
