language: rails
bundler_args: --without development
rvm:
  - 1.8.7
  - 1.9.2
  - 1.9.3
before_script:
  - "psql -c 'create database latter_test;' -U postgres"
  - "export DISPLAY=:99.0"
  - "sh -e /etc/init.d/xvfb start"
script: RACK_ENV=test bundle exec rspec spec
